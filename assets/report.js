/*
To publish a new report:
1. Copy `reports/_template.html` to `reports/<slug>.html` and set `data-report-slug`.
2. Create `assets/reports/<slug>.json` using the required report schema.
3. Add a manifest entry in `assets/data/reports.json` so it appears in `reports/index.html`.
*/
